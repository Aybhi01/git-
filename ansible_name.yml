---
 - name: Learning to apply ansible 
   hosts: localhost
   gatherfacts: false
   become: true
   tasks:
   - name: The Name of os family
     debug:
       msg: "This is {{ansible_os_family}}"
   - name: The name of inventory 
     debug:
       msg: "This is {{inventory_hostname}}"
   - name: The name of system
     debug: 
       msg: "This is {{ansible_system}}"
     
     
 
